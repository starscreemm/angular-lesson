// Modules - every file is module by default
const names = require("./5-names");
const sayHi = require("./4-sayhi");
require("./7-mind-grenade"); // invoke the code: module is wrapped into function and invoke it when require runs;
// sayHi(names.john);
// sayHi(names.peter);

// console.log(module);
